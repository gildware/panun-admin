# Copy to soketi.env on the server (same folder as start-soketi.sh).
# Values must match panun-admin/.env PUSHER_* keys.

SOKETI_DEBUG=0
SOKETI_PORT=6001

PUSHER_APP_ID=panun-app
PUSHER_APP_KEY=panun-key
PUSHER_APP_SECRET=panun-secret
